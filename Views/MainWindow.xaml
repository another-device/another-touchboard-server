<Window x:Class="AnotherTouchboard.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AnotherTouchboard"
        mc:Ignorable="d"
        Title="Another Touchboard" Height="500" Width="800" Closing="Window_Closing">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,10">
            Find Me At:
            <Hyperlink NavigateUri="https://github.com/another-device/another-touchboard-server" 
                    RequestNavigate="Hyperlink_RequestNavigate"
                    Foreground="Green">
                another-device/another-touchboard-server@github
            </Hyperlink>
        </TextBlock>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Log" 
                               FontSize="14" 
                               FontWeight="Medium" 
                               Margin="5"/>
                    
                    <TextBox Grid.Row="1" 
                             Text="{Binding LogText}" 
                             IsReadOnly="True" 
                             TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto"
                             Margin="5"/>
                </Grid>
            </Border>
        </Grid>

        <!-- 侧边栏 -->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="5"/>
            </Grid.RowDefinitions>
            
            <!-- 当前按下的键 -->
            <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="0,0,0,5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Pressed Keys" 
                               FontSize="14" 
                               FontWeight="Medium" 
                               Margin="5"/>
                    
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding PressedKeys}" 
                             Margin="5"/>
                </Grid>
            </Border>
            
            <!-- 连接的客户端 -->
            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="0,5,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Connected Clients" 
                               FontSize="14" 
                               FontWeight="Medium" 
                               Margin="5"/>
                    
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding ConnectedClients}" 
                             Margin="5"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
    